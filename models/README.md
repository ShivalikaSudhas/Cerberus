# Machine Learning Models Directory

This directory is reserved for custom weights, Haar Cascade XML files, or ONNX deep learning face detection models (e.g. YuNet / MobileNet SSD / OpenCV DNN models).

By default, OpenCV's built-in Haar Cascade classifier and LBPH recognizer are loaded automatically.
